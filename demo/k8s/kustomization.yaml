apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: proxima
resources:
  - namespace.yaml
  - configmap.yaml
  - shard-0-americas.yaml
  - shard-1-europe.yaml
  - shard-2-asia.yaml
# Secrets are NOT included here — apply separately per environment:
#   kubectl apply -f demo/k8s/secrets.yaml   (your filled-in copy of secrets-template.yaml)
# See secrets-template.yaml for per-provider instructions.
